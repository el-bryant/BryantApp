<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BryantApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BryantApp" parent="Base.Theme.BryantApp" />

    <style name="botonNaranja" parent="Theme.BryantApp">
        <item name="backgroundTint">@color/naranja</item>
    </style>

    <style name="input" parent="Theme.BryantApp">
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:layout_marginBottom">15dp</item>
        <item name="android:layout_marginStart">15dp</item>
        <item name="android:layout_marginEnd">15dp</item>
        <item name="android:textColorHint">@color/naranja</item>
        <item name="boxStrokeWidth">0.5dp</item>
        <item name="boxBackgroundColor">#F1F1F1</item>
    </style>
</resources>